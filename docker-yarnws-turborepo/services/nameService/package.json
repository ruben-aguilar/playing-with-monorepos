{
  "name": "name-service",
  "version": "1.0.0",
  "main": "index.js",
  "author": "ruben",
  "license": "MIT",
  "dependencies": {
    "@hapi/hapi": "^20.2.2"
  },
  "scripts": {
    "start": "docker run -it -p 3001:3001 name-service",
    "build": "cp ../../yarn.lock . && docker build -t name-service . && rm yarn.lock",
    "dev": "nodemon -L index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}